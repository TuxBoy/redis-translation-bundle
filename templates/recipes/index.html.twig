{% extends 'base.html.twig' %}
{% trans_default_domain 'app' %}

{% block body %}

    <h1>{{ 'recipe_index'|trans }}</h1>

    <ul>
        {% for recipe in recipes %}
            <li>{{ recipe.name }}</li>
        {% endfor %}
    </ul>

{% endblock %}